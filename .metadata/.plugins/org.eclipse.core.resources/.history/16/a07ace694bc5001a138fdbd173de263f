package com.example.demo;

import org.apache.logging.log4j.Logger;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Pruebauno1Application {

    private static final Logger logger = Logger.getLogger(Pruebauno1Application.class);

    public static void main(String[] args) {
	System.getProperties().put("server.port", 8184);
	SpringApplication.run(Pruebauno1Application.class, args);
    }

}
